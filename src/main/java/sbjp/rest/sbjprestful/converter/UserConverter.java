package sbjp.rest.sbjprestful.converter;

import org.modelmapper.ModelMapper;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;
import org.springframework.stereotype.Service;

import sbjp.rest.sbjprestful.clientsever.request.UserRequest;
import sbjp.rest.sbjprestful.entities.User;
@Component
public class UserConverter {
	
	/*
	 * private final ModelMapper modelMapper;
	 * 
	 * @Autowired public UserConverter(ModelMapper modelMapper) {
	 * this.modelMapper=modelMapper; }
	 * 
	 * public User converToRequest(UserRequest userRequest) {
	 * 
	 * User user = modelMapper.map(userRequest, User.class); return user; }
	 */
}
